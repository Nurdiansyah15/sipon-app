name: sipon_app
description: "Sipon (IKHLAS) mobile app."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  go_router: ^17.2.3
  dio: ^5.9.2
  fpdart: ^1.2.0
  flutter_dotenv: ^6.0.1
  shared_preferences: ^2.5.5
  cached_network_image: ^3.4.1
  intl: ^0.20.2
  firebase_core: ^4.1.0
  firebase_messaging: ^16.0.0
  flutter_local_notifications: ^19.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
